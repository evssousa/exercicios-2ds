# 📝 Atividade

Crie um arquivo `index.js` com o seguinte:

1. Crie uma função `multiplicar(a, b)` que **retorne** o resultado da multiplicação.
2. Crie uma função `mostrarResultado()` que use `multiplicar()` e exiba o resultado no console.
3. No final, chame `mostrarResultado()`.

🖥️ Saída esperada:

```
Resultado da multiplicação: 20

```
# 📝 Atividade

Crie um arquivo `index.js` com o seguinte:

1. Crie uma função chamada `boasVindas` que exibe a mensagem `"Bem-vindo(a) ao sistema!"`.
2. Crie uma função chamada `soma` que recebe **dois números** e mostra no console o **resultado da soma**.
3. Chame as duas funções no final do código.

🖥️ Saída esperada:

```
Bem-vindo(a) ao sistema!
Resultado da soma: 10
```
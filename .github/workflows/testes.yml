name: Testes por exercício

on:
  push:
  workflow_dispatch:

jobs:
  test-ex01:
    name: ex01-ola-mundo
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ex01-ola-mundo
    steps:
      - uses: actions/checkout@v3
      - run: npm install
      - run: npm test

  test-ex02:
    name: ex02-variaveis
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ex02-variaveis
    steps:
      - uses: actions/checkout@v3
      - run: npm install
      - run: npm test
